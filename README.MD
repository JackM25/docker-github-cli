# Unofficial GitHub CLI Image

Unofficial GitHub CLI Docker Image, based on Apline.

This image is primarily designed for use in CI/CD pipelines, so is lightweight but includes useful tools such as:
*  `curl`
*  `gzip`
*  `tar`
*  `wget`